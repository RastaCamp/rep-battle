{
  "name": "rep-battle-web",
  "private": true,
  "scripts": {
    "build": "node scripts/build-web.mjs",
    "deploy": "npm run build && npx wrangler pages deploy build/web --project-name=rastacamp-rep-battle --branch=master"
  },
  "devDependencies": {
    "wrangler": "^4.67.0"
  }
}
